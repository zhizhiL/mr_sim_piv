numpy
scipy
matplotlib
imageio          # movie output (drivers/make_movie.py)
imageio-ffmpeg   # bundles a static ffmpeg for .mp4 encoding
# The DigiFlow .dfi reader (../digiflowio.py) and vortex_core (../vortex_core.py)
# need only numpy + scipy. Install into the repo venv: .venv/bin/pip install -r requirements.txt
